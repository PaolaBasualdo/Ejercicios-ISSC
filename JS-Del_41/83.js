/*83. Define una función llamada `esPar` que tome un número como argumento y devuelva `true` si es par o `false` si es impar.*/
function esPar(num1){
    if (num1%2 ===0){
        return true;
    }else {
        return false;
    }
}
console.log(esPar(9))
